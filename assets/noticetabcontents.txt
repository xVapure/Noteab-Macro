# v2.1.3, what's new?
## Bug fixes:
* Reduced macro startup time, OCR readings instead of hosting it directly on your PC now sends a request to EasyOCR API.
    * Since EasyOCR isn't bundled into the app anymore the macro is wayyy more lightweighted now.
* Duplication merchant count logic in macro resolved.
* Attempted at fixing using item usage softlock (Using the br/sc thrice).
* Fixed the issue with "Disconnected" logs reading, it will not read old log files anymore... (so it won't falsely flag you as DIsconnected from logs anymore).
* Fixed the issue with rare biomes confirmation popup, it will only popup in the first 1 minute of reconnection now.
## Misc changes:
* Specificed the fishing delay in fishing page is in miliseconds.
# v2.1.2 (mainly bug fixes)
## New features:
- Added mouse drag down move before start Flarg selling because sometimes it clicks on Flarg's dialogue box 😭
## Bug fixes:
- Remote access control now works.
- Attempted a fix at WebView2 bug for some users (the macro black screen).
- Attempted a fix at anti ghost server detection.
- Fixed auto add not appearing in potion crafting calibrations.
- Changed the rare biomes popup when forgetting to turn off macro logic.
  - Timeout/wait interval: 10s instead of 60s.
  - No response after timeout: ping for rare biome.
# v2.1.1 full release (preview)
## Main features:
- Added fishing mode (currently only available to VIP/VIP+ Sol's gamepass owners).
    - Added use BR/SC every x fish.
    - Added auto merchant + fishing.
    - Fishing failsafe (must have reconnect to ps enabled, if not the macro will prompt a warning to the user).
    - Fishing path reworked for the **5th time**, fishing should work in basically ANY regular FPS now.
    - Added fishing mouse click delay.
    - Added equip an aura before pathing.
- Added auto pop in Cyberspace biome.
- Added default calibrations for `1920*1080 full screen 100% scale` for first time users/users with empty config.
- Added anti ghost server, basically when using the Coteab auto reconnect feature it will now detect if Sol's RNG put u on it's rejoin mode which leads to ghost servers (enabled when you enable auto reconnect to PS).
## Systemic changes:
- Proper back-end and front-end workspace structure for easier codeflow and manipulation.
    - Macro is now written in typescript, html, css for front-end and the entire back-end structure is written in Python with local JSON data-set handling.
- Reverted to the traditional `.exe` file type.
- Auto update now is embedded into the executable file.
## Misc changes:
- UI/front-end rework.
- Reworked and added new developers to credits tab.
- Calibration guide added.
## Known issues/bugs:
- RAC not working.
